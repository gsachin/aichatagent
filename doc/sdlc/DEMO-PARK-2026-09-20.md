# DEMO PARK — 2026-09-20 (late session, PDT)

> **Purpose:** this file is the demo runbook AND tomorrow's resume point. Read
> it first, then `doc/sdlc/sdlc-completion-drive-status.md` and
> `doc/sdlc/session-handoff-2026-09-19.md` for the full work log.
> Branch `perf/us007-boot-readiness`. HEAD = `a791272`.

---

## 1. Are we ready for demo? — YES, for the call experience + latency story

**What is demoable right now:**
- The live stack serves real end-to-end calls (STT → RAG/MCP → LLM → TTS).
- `/health` (liveness) and `/ready` (the boot gate's verdict, all 4 clauses) are live.
- The latency levers are built and measured — with the streams ON the median
  first-audio is **~1.3 s** vs **2.45 s** on the batch path, on the same box,
  same fixtures, same harness. The side-by-side is audible.

**Two framing lines the demo MUST carry (do not let the demo claim otherwise):**
1. The 3,000 ms cap is **not yet met at zero** — best measured so far: **7% over**
   (p50 1,328 · p90 2,187 · p95 ~7.3 s; the p95 tail is retrieval/LLM contention).
2. Streaming TTS/LLM are **behind flags, adoption pending DG-03 ground truths** —
   a PO-approved build preview (your own Phase 4a rulings), not adopted behavior.

**What the demo should NOT attempt:** SDLC-dashboard claims (story closures /
traceability are mid-flight), cap-compliance claims, quality/ground-truth claims
(US-003 unfrozen).

## 2. Current stack state (left running, ready for demo)

| Service | Port | State |
|---|---|---|
| Ollama | :11434 | up; `llama3.2:3b` + `nomic-embed-text` resident, held leases (A5) |
| FastAPI | :8000 | **running with `TTS_STREAM=1 LLM_STREAM=1`** (demo mode) + the US-008 shared client |
| ERC MCP | :8010 | up, embed lease held (`EMBED_KEEP_ALIVE`; uncommitted in the ERC repo — flagged) |
| CRM / streamlit | :8098 / :8501/:8502 | down — start via `start_services.ps1` only if the demo needs them |

Verify before the demo: `curl http://127.0.0.1:8000/ready` → `"ready": true`.
Use `127.0.0.1`, never `localhost` (IPv6 stall on this box).

## 3. Demo runbook (suggested 15-minute shape)

1. **Readiness screen:** `GET /ready` — show the 4 clauses + `status: "ready"`.
   The story: "the stack refuses to call itself ready until the model is
   resident, the prefix is warm (engine-confirmed), and every config key has
   a reader — read at call time, not assumed from a boot log line."
2. **The baseline side-by-side (the money shot):**
   - Batch mode: stop the app, start with `TTS_STREAM=0 LLM_STREAM=0`, play one
     turn → first audio ≈ **2.4–2.6 s**.
   - Stream mode: restart with `TTS_STREAM=1 LLM_STREAM=1`, same question →
     first audio ≈ **1.3–1.6 s**. The caller HEARS the difference.
   - Mechanics to say: "the answer is cut into clauses and synthesised while
     the model is still decoding — the first sentence is spoken ~440 ms after
     generation starts, and TTFT is ~266 ms. The batch path is untouched; the
     flag is the whole rollback."
   - Drive turns with: `.venv/Scripts/python.exe doc/perf/tools/load_harness.py
     --n 1 --turns 3 --readiness-url http://127.0.0.1:8000/ready`
     (or a real Twilio call if the tunnel is up).
3. **The measured record (1 slide):** batch p50 2,453 / p90 3,891 / 27% over-cap
   → dual-stream p50 **1,328** / p90 **2,187** / **7%** over-cap (N=2 warm, same
   fixtures). Every run summary records `cap_basis` (harness clock, PO-ruled),
   both clocks per turn, and the ping/readiness settings.
4. **The honesty beat:** p95 ≈ 7.3 s — the remaining tail is retrieval/LLM
   contention; next levers are already queued (ERC `asyncio.to_thread`, Kokoro
   pool, US-008 acceptance run).

## 4. Resume context for tomorrow

**Done this session (commits, in order):**
`102a04e` A2 dual clocks · `b4ca40a` Phase 0.4 WS-1011 root-cause + harness
ping knobs · `c8a31b6` Phase 1.1 `/health` + `/ready` · `b9d3da7` A5 14B orphan
+ embed lease · `d176be3` A3 breaker on `/api/perf/policy` · `8ae8ff4` Phase 4a
six PO rulings · `43a3fb9` US-005 streaming TTS · `96f3407` US-004 streaming LLM
+ harness ping-kwarg fix · `3a1a101` **US-004 freeze root-cause** (ClauseCutter
infinite loop — py-spy evidence) · `55a356b` docs · `a791272` US-008 shared
client re-merge.

**Pending, in order (full detail: drive-status file):**
1. Phase 2.3 qualifying runs — the 0-over-cap bar (currently 7% over).
2. Tail levers: ERC `asyncio.to_thread` (uncommitted in ERC repo — needs your
   commit decision), Kokoro dedicated pool (2–3 workers).
3. Phase 1.2 US-011 TAC-1 migration · Phase 1.3 T-11 trace fields.
4. Phase 3 closures: US-008 acceptance suite (T-1..T-14), US-012/016/017/001/
   015/018 · Phase 4b US-003 signoff sittings (15 intents) · Phase 5 traceability
   /doc-truth · Phase 6 `run_all_gates.py`.

**Known caveats for tomorrow:** the harness framing audit still flakes on host
noise (Phase 3.1 scope); `salesforceAPISchema` has uncommitted changes not ours
(salesforce workstream — leave untouched); ERC-repo changes are flagged, never
committed without you.

**Gates, all green as parked:** hygiene 3/3 · doc-truth 7/7 · US-004 19/19 ·
US-005 25/25 · US-007 40/40 · US-013 51/51 · US-008 shared-client 5/5 ·
US-006 12/12 · harness self-test (summary contract incl. `a1/a2/ws_ping` all
True; framing audit = known host flake).
