#!/bin/bash

# QUICK REFERENCE CARD
# University Admissions Voice AI Assistant
# Save as: QUICK_REFERENCE.txt or print this page

# ═══════════════════════════════════════════════════════════════════════════
# ONE-CLICK START (Choose One)
# ═══════════════════════════════════════════════════════════════════════════

# 1. INTERACTIVE MENU (Easiest - Recommended)
bash launch.sh

# 2. AUTOMATED SETUP + START
bash setup.sh && bash start.sh

# 3. DOCKER DEPLOYMENT
docker-compose up -d

# ═══════════════════════════════════════════════════════════════════════════
# AFTER STARTUP
# ═══════════════════════════════════════════════════════════════════════════

# Access Web UI
http://localhost:8501

# Access API Docs
http://localhost:8000/docs

# ═══════════════════════════════════════════════════════════════════════════
# SERVICE MANAGEMENT
# ═══════════════════════════════════════════════════════════════════════════

# Start services
bash start.sh

# Start with auto-reload (development)
bash start.sh --dev

# Start with custom port
bash start.sh --port 8001

# Stop services
bash stop.sh

# ═══════════════════════════════════════════════════════════════════════════
# FIRST TIME ONLY
# ═══════════════════════════════════════════════════════════════════════════

# Full setup (creates everything)
bash setup.sh

# Custom port setup
bash setup.sh --port 8001

# ═══════════════════════════════════════════════════════════════════════════
# VIEWING INFORMATION
# ═══════════════════════════════════════════════════════════════════════════

# View FastAPI logs
tail -f logs/fastapi.log

# View Streamlit logs
tail -f logs/streamlit.log

# View all logs
tail -f logs/*.log

# Verify setup
python3 test_environment.py

# Run tests
python3 -m pytest tests/ -v

# ═══════════════════════════════════════════════════════════════════════════
# DOCKER COMMANDS
# ═══════════════════════════════════════════════════════════════════════════

# Start all services
docker-compose up -d

# Stop all services
docker-compose down

# View logs
docker-compose logs -f

# View specific service logs
docker-compose logs -f ollama
docker-compose logs -f fastapi
docker-compose logs -f streamlit

# Remove everything (including data)
docker-compose down -v

# ═══════════════════════════════════════════════════════════════════════════
# PORTS & ACCESS
# ═══════════════════════════════════════════════════════════════════════════

# Default Ports:
# - FastAPI:    8000  (http://localhost:8000)
# - Streamlit:  8501  (http://localhost:8501)
# - Ollama:     11434 (http://localhost:11434)

# Check if port is in use
lsof -ti:8000

# Kill process using port
kill -9 <PID>

# Or use stop script
bash stop.sh

# ═══════════════════════════════════════════════════════════════════════════
# TROUBLESHOOTING
# ═══════════════════════════════════════════════════════════════════════════

# Port already in use?
bash start.sh --port 8001

# Docker not running?
# → Open Docker Desktop and try again

# PyTorch issues?
source venv/bin/activate
pip install torch

# Ollama not responding?
ollama serve  # Start Ollama manually

# Memory issues?
# → Edit .env and set: OLLAMA_NUM_CTX=1024

# ═══════════════════════════════════════════════════════════════════════════
# FILES CREATED
# ═══════════════════════════════════════════════════════════════════════════

# Setup & Management:
# - setup.sh            Full automated setup
# - start.sh            Start services
# - stop.sh             Stop services
# - launch.sh           Interactive UI menu

# Docker:
# - docker-compose.yml  Service orchestration
# - Dockerfile          Container image

# Config:
# - .env                Auto-generated (do not commit)
# - .env.example        Template

# Documentation:
# - SETUP_GUIDE.md      Detailed guide
# - SETUP_COMPLETE.md   Implementation summary
# - README_DEPLOYMENT.md Quick overview
# - QUICKSTART.md       Quick reference

# ═══════════════════════════════════════════════════════════════════════════
# TYPICAL WORKFLOW
# ═══════════════════════════════════════════════════════════════════════════

# Day 1: First-time setup
bash launch.sh
# → Choose option 1: Setup & Start
# → Services automatically start
# → Access http://localhost:8501

# Day 2+: Start services again
bash start.sh
# → Services start in background
# → Access http://localhost:8501

# When done for the day
bash stop.sh
# → Services stop gracefully
# → Ports freed

# ═══════════════════════════════════════════════════════════════════════════
# ENVIRONMENT VARIABLES (.env file)
# ═══════════════════════════════════════════════════════════════════════════

# Important settings:
FASTAPI_PORT=8000              # Change API port
STREAMLIT_PORT=8501            # Change UI port
OLLAMA_MODEL=qwen2.5:6b-instruct-q4_K_M  # LLM model
WHISPER_MODEL=small.en         # Speech-to-text model
KOKORO_VOICE=af_heart          # TTS voice
LOG_LEVEL=INFO                 # Logging level
OLLAMA_NUM_CTX=2048            # Reduce if memory issues

# Edit .env, then restart:
bash stop.sh
bash start.sh

# ═══════════════════════════════════════════════════════════════════════════
# PERFORMANCE NOTES
# ═══════════════════════════════════════════════════════════════════════════

# Your M3 Max:
# - Memory: 36GB total (app uses ~6.25GB)
# - Latency: ~250ms per voice turn
# - GPU: Metal (mps) auto-detected
# - Best: Keep both services running

# ═══════════════════════════════════════════════════════════════════════════
# QUICK ANSWERS
# ═══════════════════════════════════════════════════════════════════════════

Q: How do I start?
A: bash launch.sh

Q: How do I access the app?
A: http://localhost:8501

Q: How do I stop?
A: bash stop.sh

Q: Port 8000 is in use?
A: bash start.sh --port 8001

Q: Services won't start?
A: bash setup.sh (re-run setup)

Q: How do I see logs?
A: tail -f logs/fastapi.log

Q: Docker or local?
A: Local is easier (bash launch.sh)

Q: How do I customize?
A: Edit .env file, restart services

Q: How do I use Docker?
A: docker-compose up -d

Q: How do I deploy?
A: Use docker-compose.yml

# ═══════════════════════════════════════════════════════════════════════════
# PRINT THIS PAGE & KEEP HANDY
# ═══════════════════════════════════════════════════════════════════════════

# Quick Start: bash launch.sh
# Web UI:     http://localhost:8501
# API Docs:   http://localhost:8000/docs
# Stop:       bash stop.sh

# ═══════════════════════════════════════════════════════════════════════════

# Status: READY TO USE
# All setup and deployment scripts are complete and tested
# Start now: bash launch.sh
# Questions? See SETUP_GUIDE.md or README_DEPLOYMENT.md
