# University Admissions Voice Assistant — Python dependencies
# Install: pip install -r requirements.txt

# ── Web framework + ASGI server ──────────────────────────────────────
fastapi
uvicorn[standard]
streamlit

# ── WebSocket (client + server) ──────────────────────────────────────
websockets

# ── Async HTTP (used by test suite) ──────────────────────────────────
httpx

# ── AI / ML Inference ────────────────────────────────────────────────
torch
ollama
chromadb

# ONNX Runtime — GPU version for CUDA-accelerated Kokoro TTS (~2x speedup).
# Swap to 'onnxruntime' (CPU-only) if no NVIDIA GPU is available.
onnxruntime-gpu

# ── Speech-to-Text ────────────────────────────────────────────────────
# openai-whisper used for WhatsApp voice note transcription.
# Avoids faster-whisper's PyAV DLL blocked by AppLocker on this machine.
openai-whisper

# ── Voice AI Pipeline (Pipecat) ──────────────────────────────────────
pipecat-ai[whisper,kokoro]

# ── Database (lead capture — Phase 6) ────────────────────────────────
psycopg2-binary
