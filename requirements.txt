# Pinned 2026-08-15 — exact versions from the verified-working environment
# (Python 3.11, Windows 11 + CUDA; same pins apply to macOS/Linux).
#
# GPU notes:
# - torch>=2.7.0,<2.8 below is the PyPI (CPU) build. On Windows NVIDIA boxes
#   bootstrap_services.py installs torch==2.7.1+cu128 from
#   https://download.pytorch.org/whl/cu128 first — REQUIRED for RTX 50-series
#   (Blackwell sm_120) GPUs, which torch 2.6.x/cu124 cannot use.
# - Swap onnxruntime-gpu for onnxruntime on CPU-only machines.

# ── Web / API ─────────────────────────────────────────────────────────
fastapi==0.140.0
uvicorn[standard]==0.51.0
streamlit==1.59.2
websockets==15.0.1
httpx==0.28.1
python-multipart==0.0.32
requests==2.34.2
python-dotenv==1.2.2

# ── ML / audio ────────────────────────────────────────────────────────
torch>=2.7.0,<2.8
# GPU-capable onnxruntime ships no macOS wheels — plain build on Darwin.
# Darwin is pinned to 1.24.x because pipecat-ai 1.6.0 requires onnxruntime~=1.24.3.
onnxruntime-gpu==1.28.0; platform_system=="Windows" or platform_system=="Linux"
onnxruntime==1.24.3; platform_system=="Darwin"
numpy==2.4.6
scipy==1.17.1
soundfile==0.14.0
faster-whisper==1.2.1
kokoro-onnx==0.5.0
openai-whisper==20250625
pipecat-ai[whisper,kokoro]==1.6.0

# ── RAG / LLM ─────────────────────────────────────────────────────────
ollama==0.6.2
chromadb==1.5.9
pandas==3.0.3
pyarrow==24.0.0
langchain==1.3.14
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-ollama==1.1.0
langchain-classic==1.0.8
pypdf==6.14.2

# ── DB / integrations ─────────────────────────────────────────────────
psycopg2-binary==2.9.12
twilio==9.10.9
fpdf2==2.8.7

# ── Optional: MCP server (app/mcp/server.py) — not required for voice/chat/UI.
mcp==2.0.0

# ── macOS Apple Silicon (MLX backend; replaces Ollama for LLM on Mac) ──
# Windows/Linux keep using Ollama (see app/llm_backend.py). Versions pinned
# from the verified macOS install (pip-resolved 2026-08-15).
mlx-lm==0.31.3; platform_system=="Darwin"
langchain-openai==1.5.1; platform_system=="Darwin"
openai==2.54.0; platform_system=="Darwin"
einops==0.8.2; platform_system=="Darwin"
mlx-whisper==0.4.3; platform_system=="Darwin"
torchvision==0.21.0; platform_system=="Darwin"
